# Generate a unique value for each deployment. Never commit the real .env file.
POSTGRES_PASSWORD=replace-with-a-long-random-password

# Local Compose defaults to http://localhost. Set this to the public hostname in production.
API_HOST=http://localhost
