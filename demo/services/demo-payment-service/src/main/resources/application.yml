spring:
  application:
    name: demo-payment-service
server:
  port: 8090
causora:
  telemetry-base-url: ${TELEMETRY_BASE_URL:http://localhost:8081}
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  metrics:
    tags:
      application: ${spring.application.name}
